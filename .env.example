# Orígenes CORS permitidos para acceso entre dispositivos (separados por coma).
# Incluye la IP LAN de tu PC y los puertos del frontend web (dev 3000, preview 5173).
CORS_ORIGINS=http://localhost:3000,http://192.168.X.X:3000,http://192.168.X.X:5173
